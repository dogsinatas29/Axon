[Core CoT]
1. 사용자의 질문 "이건 spec.md의 문제인가 그러면 그 부분을 고치면 되. 로직의 문제라면 문제가 되는 부분을 확인해보자"에 대응하기 위해 `ProjectIR::from_md` 구현부와 `spec.md`, `architecture.md` 구조를 분석함.
2. 분석 결과:
   - `crates/axon-ir/src/schema/types.rs`에 정의된 `ProjectIR::from_md` 파서는 `<!-- AXON:SPEC:COMPONENTS` 태그 내부의 JSON 데이터를 파싱하기 위해 `RawComponent` 구조체로 역직렬화(Deserialize)를 시도함.
   - `RawComponent` 구조체는 `symbols: Vec<String>` 필드를 **필수(Required)** 필드로 요구하고 있음.
   - 그러나 부트스트랩 단계에서 데몬에 의해 자동 생성된 `architecture.md` 하단 JSON 블록에는 `symbols` 필드 대신 `functions` 필드가 기재되어 있어, JSON 파싱 시 `symbols` 누락 에러로 인해 `ProjectIR` 추출이 실패(`None` 반환)하고 `[IR_PARSE_FAIL]` 경고가 발생함.
3. 따라서 이 에러는 `spec.md` 단독의 문제가 아닌, 데몬 런타임/파서(`axon-ir`과 `axon-daemon`) 간의 **스키마 설계 불일치(로직 문제)**로 규정함.
4. 사용자에게 분석된 버그 메커니즘을 시니어 엔지니어의 시각으로 상세히 해설함.

[Affected Files Diff]
None (No code changes in this turn, gate file generated for process recording)
