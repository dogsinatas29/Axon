# Win32 Window Procedure Contract
REQUIRED_SYMBOLS: WndProc, DefWindowProc, RegisterClass, CreateWindow
DESCRIPTION: Application must register a window class and implement a WndProc callback linked to DefWindowProc.
