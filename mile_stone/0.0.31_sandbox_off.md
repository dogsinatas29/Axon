# AXON v0.0.31_sandbox_off — Universal Constitutional IR Enforcement Spec

## Identity

AXON is no longer a sandbox-trusting autonomous code generator.

It is now:

```text
A constitution-driven deterministic software factory
that treats all LLM output as untrusted until validated,
patched,
or rejected
by platform-aware structural enforcement layers.
```

---

# 0. CORE PHILOSOPHY SHIFT

## Legacy Model (Deprecated)

```text
LLM
 ↓
Generate Files
 ↓
Build
```

Problem:

* hallucinated architecture
* interface drift
* invalid entrypoints
* generic POSIX bias
* validator bypass
* full-file rewrites
* broken headers
* unstable regeneration loops

---

## Constitutional Model (Mandatory)

```text
LLM
 ↓
IR Generation
 ↓
Constitution Validator
 ↓
Surgical Patch Engine
 ↓
Platform Enforcement
 ↓
Deterministic Build
```

---

# 1. SANDBOX MODEL DEPRECATION

IMPORTANT.

The old sandbox-first architecture is officially deprecated.

---

## Deprecated Assumption

```text
"LLM can safely regenerate files inside sandbox repeatedly."
```

This assumption is now considered invalid.

Reason:

* verified code destruction
* validator bypass loops
* unstable rewrites
* interface drift
* architectural collapse
* recursive hallucination amplification

---

## New Rule

```text
Sandbox execution is optional.

Constitutional validation is mandatory.
```

---

# 2. UNIVERSAL IR CONSTITUTION

IMPORTANT.

ALL language pipelines MUST obey the same constitutional architecture.

This includes:

```text
- C
- C++
- Rust
- Python
- Win32
- POSIX
- CLI
- GUI
```

NO exception.

---

# 3. UNIVERSAL PROGRAM SPECIES MODEL

The IR schema MUST explicitly define what kind of program is being generated.

---

## Required Runtime Identity

```rust
pub enum RuntimeSubsystem {
    Console,
    WindowsGui,
    Posix,
    Embedded,
}
```

---

## Required ABI Identity

```rust
pub enum AbiType {
    Win32,
    Posix,
    Native,
}
```

---

## Required Entrypoint Identity

```rust
pub enum EntrypointType {
    Main,
    WinMain,
    WWinMain,
    RustMain,
    PythonModule,
}
```

---

## Required Runtime Model

```rust
pub enum RuntimeModel {
    EventDriven,
    Polling,
    AsyncRuntime,
    EmbeddedLoop,
}
```

---

# 4. UNIVERSAL COMPONENT ONTOLOGY

IMPORTANT.

LLMs MUST NOT invent architecture blindly.

The IR ontology MUST explicitly define structural species.

---

## Universal Component Types

```rust
pub enum ComponentType {
    Module,
    Runtime,
    Renderer,
    TextBuffer,
    Validator,
    Dispatcher,
    Storage,
    ApiServer,
    LuaRuntime,
}
```

---

## Win32-Specific Types

```rust
pub enum Win32ComponentType {
    Win32WindowClass,
    Win32WndProc,
    Win32MessageLoop,
    Win32Dialog,
    Win32Resource,
}
```

---

## Rust-Specific Types

```rust
pub enum RustComponentType {
    TokioRuntime,
    AxumServer,
    TraitContract,
    AsyncDispatcher,
}
```

---

## Python-Specific Types

```rust
pub enum PythonComponentType {
    PythonModule,
    FastApiServer,
    PythonEntrypoint,
}
```

---

# 5. ONTOLOGY INTERCEPTOR LAYER

IMPORTANT.

The generator MUST become platform-aware BEFORE generation begins.

---

## Ontology Trigger

If the specification contains:

```text
HWND
WndProc
WM_PAINT
Win32
GetMessage
DispatchMessage
```

the pipeline MUST force:

```text
language = C++
subsystem = WindowsGui
entrypoint = WWinMain
abi = Win32
runtime_model = EventDriven
```

---

## Rust Trigger

If the specification contains:

```text
tokio
axum
warp
async fn
```

the pipeline MUST force:

```text
runtime_model = AsyncRuntime
language = Rust
```

---

## Python Trigger

If the specification contains:

```text
FastAPI
Flask
uvicorn
asyncio
```

the pipeline MUST force:

```text
language = Python
runtime_model = AsyncRuntime
```

---

# 6. HARD REJECTION SYSTEM

IMPORTANT.

Certain violations MUST immediately terminate the pipeline.

---

## Win32 Hard Rejects

The following MUST trigger immediate rejection:

```text
- int main(void)
- src/main.c
- user32.c
- gdi.c
- kernel32.c
- fake Win32 implementations
```

---

## Rust Hard Rejects

Reject if:

```text
- blocking runtime inside tokio runtime
- giant trait explosion
- ECS hallucination
- async recursion loops
```

---

## Python Hard Rejects

Reject if:

```text
- circular imports
- runtime monkey patching
- dynamic dependency installers
- hidden subprocess loops
```

---

# 7. SURGICAL PATCH ENGINE

IMPORTANT.

Full-file regeneration is now prohibited for verified files.

---

## Forbidden

```text
- rewrite entire file
- regenerate validated modules
- replace stable interfaces
```

---

## Mandatory Behavior

Only pinpoint modifications are allowed.

Examples:

```diff
- int main(void)
+ int WINAPI wWinMain(HINSTANCE, HINSTANCE, PWSTR, int)
```

```diff
+ #include <windows.h>
```

---

## Patch Scope Rules

Allowed:

```text
- line replacement
- include injection
- function signature correction
- local interface patch
```

Forbidden:

```text
- architectural rewrite
- file-wide replacement
- component topology mutation
```

---

# 8. HEADER AUTO-INJECTION LAYER

IMPORTANT.

The pipeline MUST automatically repair missing platform headers.

---

## Win32 Header Injection

If tokens include:

```text
HWND
HINSTANCE
WPARAM
LPARAM
WNDCLASS
WNDPROC
```

then:

```cpp
#include <windows.h>
```

MUST be inserted automatically.

---

## Rust Injection

If tokens include:

```text
tokio::spawn
```

but Cargo.toml lacks tokio dependency:

AUTO PATCH:

```toml
tokio = { version = "1", features = ["full"] }
```

---

## Python Injection

If:

```text
FastAPI
```

exists without imports:

AUTO PATCH:

```python
from fastapi import FastAPI
```

---

# 9. VALIDATOR SUPREMACY

IMPORTANT.

Validators are now sovereign over generators.

---

## New Authority Model

```text
Generator < Validator
```

---

## Validators MAY

```text
- reject IR
- patch IR
- patch source
- patch headers
- patch build scripts
- prune files
- terminate generation
```

---

## Generators MAY NOT

```text
- bypass validators
- rewrite validated code
- mutate stable interfaces
- ignore platform ontology
```

---

# 10. BUILD SYSTEM CONSTITUTION

IMPORTANT.

Build generation is no longer freeform.

---

## Win32 Mandatory Rules

```cmake
set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -mwindows")
```

Required:

```cmake
target_link_libraries(MyLightIDE
    user32
    gdi32
    kernel32
    shell32
    comdlg32
)
```

---

## Rust Mandatory Rules

Required:

```toml
edition = "2021"
```

Reject:

```text
nightly-only unstable features
```

unless explicitly allowed.

---

## Python Mandatory Rules

Reject:

```text
pip install during runtime
```

Forbidden:

```text
self-modifying dependency bootstrap
```

---

# 11. FILE OWNERSHIP CONSTITUTION

IMPORTANT.

Every file MUST have explicit ownership.

---

## Required Ownership Metadata

```rust
pub struct FileOwnership {
    pub owner_component: String,
    pub validator_locked: bool,
    pub patchable_regions: Vec<Region>,
}
```

---

## Validator Locked Files

If:

```text
validator_locked = true
```

then:

```text
full rewrite prohibited
```

---

# 12. PRUNING AUTHORITY

Validators MAY delete unconstitutional files.

---

## Examples

```text
src/main.c
src/user32.c
src/gdi.c
src/kernel32.c
```

Reason:

```text
Pseudo-system implementation violation
```

---

# 13. EVENT LOOP CONSTITUTION

IMPORTANT.

Event-driven systems MUST remain event-driven.

---

## Win32

Rendering allowed ONLY inside:

```text
WM_PAINT
```

Reject:

```text
while(running) render loop
```

---

## Async Rust

Reject:

```text
nested tokio runtimes
```

---

## Python

Reject:

```text
busy-loop polling servers
```

---

# 14. UNIVERSAL INTERFACE DRIFT PROTECTION

IMPORTANT.

Stable interfaces are constitutional assets.

---

## Forbidden

```text
- parameter mutation
- ownership mutation
- return type drift
- ABI mutation
```

without validator approval.

---

# 15. AXON PIPELINE REORDERING

Old:

```text
LLM → Build
```

New:

```text
LLM
 ↓
IR
 ↓
Ontology Interceptor
 ↓
Platform Validator
 ↓
Surgical Patch Engine
 ↓
Build Validator
 ↓
Deterministic Build
```

---

# 16. FINAL AXON DEFINITION

AXON is now:

```text
A constitutional deterministic software factory
where validators,
platform ontology,
and structural enforcement layers
hold sovereignty over all LLM-generated output.
```

Success means:

```text
Structural integrity preservation,
not autonomous generation freedom.
```

