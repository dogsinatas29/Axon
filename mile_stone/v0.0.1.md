# encoding: utf-8
# 🚀 Milestone v0.0.1 - Control & Isolation

---

## 🏛️ Sovereign Protocol - Master Hub Specification (v0.0.1)
**"모든 구현은 규약을 따르며, 모든 통합은 마스터 허브에서 증명된다."**

### 1. 단일 진실 공급원 (SSOT) 체계
- **[architecture.md](file:///home/dogsinatas/rust_project/axon/architecture.md)**: 전체 시스템의 권한 지도 및 계층 구조(Hub -> Cluster -> Node)를 총괄하는 최상위 허브입니다.
- **모듈화 스펙**:
  - `core_synapse.md`: 시각화 및 엔진 규격
  - `visual_impact.md`: 실행 및 에이전트 제어
  - `reporting.md`: 진단 및 리포트 템플릿
  - `data_scheme.md`: 데이터 스키마 및 상태 정의

### 2. 팀 기반 독재적 효율성 (Sovereign Authority)
- **Top-Down 설계**: 팀장이 `architecture.md`에서 허브(Hub)를 할당하고 구현 범위를 지정합니다.
- **분산형 구현**: 각 유저(Worker)는 할당된 허브 샌드박스 내에서 `규약.md`를 준수하며 독자적으로 로직을 구성합니다.
- **임시 버퍼 및 머지**: 구현된 내용은 `temp_architecture.md` 버퍼를 거쳐 팀장의 최종 승인 시 마스터로 승격(Promotion)됩니다.

### 3. 규속(Chain of Command)
- 제미나이(AI)는 작업 시작 시 항상 `architecture.md`와 연결된 모듈 문서들을 먼저 읽어 현재 작업 구역의 권한과 제한 사항을 파악해야 합니다.

---

## 📅 작업 정보
- **상태:** ✅ Completed
- **관련 마일스톤:** N/A (Initial consolidated milestone for v0.0.1)
- **목표:** 웹 뷰어를 통한 작업 제어권 강화 및 프로젝트 간 격리(Isolation) 구현의 안정화 및 컴파일 실현.

## 🧠 상세 설계 및 로직
1. **웹 뷰어 제어 기능 (The Control Panel)**
   - `Pause / Resume` 연동 및 `DaemonState::Committing`을 통한 작업 차단 플로우 수립.
2. **프로젝트 격리 및 별도 뷰어 (Multi-Project View)**
   - SandboxRegistry 및 sandbox_registry 필드를 통한 독립적인 세션과 라우팅 유지.

## 🛠️ 기술적 변경 사항
- **Daemon Struct:** `sandbox_registry`, `stage_notify`, `commit_generation` 필드 추가 및 생성자 동기화.
- **Coordinator:** `completed_titles` 조회 로직을 적용하여 Circular lock을 회피하는 Batch build 설계.
- **Task/Thread:** 새로운 에러 카운터(validator/senior/architecture/cargo/lsp) 및 `error_feedback` 필드들의 누락 없는 초기화.
- **Component:** `axon_ir` 라이브러리를 사용해 taxonomy 속성 (`ProjectModule` 및 `generator_patchable` 소유권) 강제 매핑.

## ⚠️ 예외 처리 및 주의 사항
- `TaskKind` 에늄 매핑 시, 다중 언어 지원을 위한 `LanguageTaskKind::C(...)` 래핑 적용.
- `server.rs` 의 `Committing` 트랜잭션 락 갱신 시 `Instant` 필드 직렬화 오류 우회 (skip 및 default 어트리뷰트 활용).

## 📝 Post-Work Log (작업 후 기록)
- 2026.05.23: 1차 컴파일 완료 및 `cargo check` 통과. `lib.rs` 및 `server.rs` 안정화 릴리즈 획득.
- 2026.05.28: `TEST4_GTK` 샌드박스의 빌드 정상화 및 소스/Lua 연동 완수. `<gtk/gtk.h>` 경로 및 `${LUA_INCLUDE_DIR}` 연동을 통한 컴파일 에러 해결. C++ 소스(`gtk_app.cpp`, `input.cpp`, `lua_runtime.cpp`) 및 Lua 스크립트(`init.lua`, `editor.lua`, `motion.lua`, `renderer.lua`) 전체 구현 완료 및 성공 빌드 완료.

