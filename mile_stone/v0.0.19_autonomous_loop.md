# AXON v0.0.19 AUTONOMOUS LOOP (TEST GENERATION + FEEDBACK)

## PURPOSE

Enable self-improving loop: code → run → fail → analyze → regenerate →
pass

------------------------------------------------------------------------

# 1. LOOP OVERVIEW

GENERATE CODE → EXECUTE → TEST → IF FAIL: → ANALYZE ERROR → GENERATE FIX
→ RETRY → SUCCESS

------------------------------------------------------------------------

# 2. ERROR CAPTURE

``` python
def extract_error(result: dict):
    if not result.get("success"):
        return result.get("stderr") or result.get("error")
    return None
```

------------------------------------------------------------------------

# 3. AUTO TEST GENERATION (LLM)

Prompt:

YOU ARE A TEST GENERATOR

INPUT: - code

OUTPUT:

\[TEST\]

``` python
def test():
    ...
```

RULES: - must be executable - must assert behavior

------------------------------------------------------------------------

# 4. TEST EXECUTION

``` python
def run_test(test_code: str, file_map: dict):
    import tempfile, os, subprocess

    with tempfile.TemporaryDirectory() as tmp:
        for fname, code in file_map.items():
            with open(os.path.join(tmp, fname), "w") as f:
                f.write(code)

        test_file = os.path.join(tmp, "test.py")
        with open(test_file, "w") as f:
            f.write(test_code)

        result = subprocess.run(
            ["python", test_file],
            capture_output=True,
            text=True
        )

        return result.returncode == 0, result.stdout, result.stderr
```

------------------------------------------------------------------------

# 5. FEEDBACK PROMPT (핵심)

``` text
YOU FAILED.

ERROR:
{error}

CODE:
{code}

FIX THE CODE.

OUTPUT FORMAT:

[OUTPUT]

FILE: <filename>

```python
<fixed code>
```


    ---

    # 6. RETRY LOOP (AUTONOMOUS)

    ```python
    def autonomous_loop(generate_fn, test_fn, max_iter=5):
        code = generate_fn()

        for i in range(max_iter):
            result = sandbox_run(code)

            if result["success"]:
                test_ok, _, _ = test_fn(code)
                if test_ok:
                    return code

            error = extract_error(result)

            code = generate_fn(feedback=error)

        raise RuntimeError("Failed after retries")

------------------------------------------------------------------------

# 7. SAFETY RULES

-   max iteration limit
-   rollback on every failure
-   no direct overwrite before pass

------------------------------------------------------------------------

# 8. SUCCESS CONDITIONS

-   execution success
-   test success
-   stable output

------------------------------------------------------------------------

# RESULT

-   self-correcting system
-   no manual debugging
-   continuous improvement loop
