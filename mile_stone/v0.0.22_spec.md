# v0.0.22_spec.md

## Goal
Deterministic IR convergence via schema-driven pipeline.

## Pipeline
Spec → Scrubber → Schema → LLM Fill → Validator → Repair → Loop

## Principle
Schema is Single Source of Truth (SSOT).
