# encoding: utf-8
# 🚀 Milestone v0.0.23 - Protocol Hardening: AXON IR-First Model

---

## 🏛️ Sovereign Protocol - Master Hub Specification (v0.2.21+)
**"모든 구현은 규약을 따르며, 모든 통합은 마스터 허브에서 증명된다."**

### 1. 단일 진실 공급원 (SSOT) 체계
- **[ARCHITECTURE_AXON.md](file:///home/dogsinatas/rust_project/axon/TEST3/GEMINI/architecture.md)**: 기술적 SSOT이자 핵심 IR입니다.
- **[IR-First Plan](file:///home/dogsinatas/.gemini/antigravity/brain/d2109169-3d02-4599-9d52-385af659f441/v0.0.23_ir_first_plan.md)**: IR 중심 실행 모델의 상세 설계안입니다.
- **[Patch-to-IR Mapping Spec](file:///home/dogsinatas/.gemini/antigravity/brain/d2109169-3d02-4599-9d52-385af659f441/axon_patch_to_ir_mapping.md)**: Patch v2 데이터를 IR로 투영하는 변환 규약입니다.
- **[Validator v2 Spec](file:///home/dogsinatas/.gemini/antigravity/brain/d2109169-3d02-4599-9d52-385af659f441/axon_validator_v2.md)**: IR 중심의 검증 기준 및 자가 치유(Self-healing) 가이드입니다.

### 2. 팀 기반 독재적 효율성 (Sovereign Authority)
- **Intent-based Implementation**: LLM은 코더가 아닌 '의도(Implementation) 생성기'로 동작합니다.
- **System-driven Generation**: 실제 파일은 업데이트된 IR로부터 시스템이 결정론적으로 생성합니다.

---

## 📅 작업 정보
- **상태:** 🚧 In-Progress (Specs Defined)
- **관련 마일스톤:** [v0.0.22](file:///home/dogsinatas/rust_project/axon/mile_stone/v0.0.22_verification_report.md)
- **목표:** 파일 직접 수정 방식의 한계를 극복하기 위해 **IR 중심 실행 모델**로 전면 전환하여 아키텍처 정합성과 심볼 무결성 확보.

## 🧠 상세 설계 및 로직 (IR-First Architecture)
### 1. IR 매핑 및 규약 강제 (Stage 6.1)
- LLM 패치를 파일에 반영하기 전, **IR Contract** 준수 여부를 먼저 검증.
- `architecture.md`에 정의된 심볼(함수)이 모두 구현되었는지, 정의되지 않은 심볼이 추가되었는지 체크.

### 2. AXON Patch v2 + IR Transformer
- LLM 출력(Patch v2) -> IR Change -> Updated IR 순서로 변경 사항 전파.
- 파일 시뮬레이션은 업데이트된 IR을 바탕으로 수행.

### 3. 고도화된 Validator (Holistic Validation)
- Syntax 뿐만 아니라 **Interface Mismatch**, **Symbol Drift**를 IR 레벨에서 차단.

## 🛠️ 기술적 변경 사항
- **axon-core (IR Schema)**: `HashMap` 기반의 `ProjectIR`, `Component`, `Function` 구조로 전면 개편.
- **axon-core (IR Engine)**: `IRChange` 열거형 및 `apply_changes` 엔진 구현.
- **axon-core (Transformer)**: Patch v2를 `IRChange`로 변환하는 `patch_to_ir_changes` 구현.
- **axon-core (Validator v2)**: 컴포넌트 존재성, 심볼 규약, 의존성 정합성을 검증하는 IR 중심 Validator 구현.
- **axon-daemon**: Stage 6.1 `IR Mapping Validation` 단계 신설 및 `axon_ir_mapper.py` 연동 (Rust 통합 대기 중).
- **axon-agent**: 시스템 프롬프트에 `IR Contract Enforcement` 지침 주입.

## ⚠️ 예외 처리 및 주의 사항
- **Symbol Drift**: LLM이 유용한 헬퍼 함수를 추가하더라도 IR에 없으면 일단 'Drift'로 간주하여 반려하거나 IR 업데이트 요청 유도.
- **Minimal Parsing**: Rust Transformer는 현재 최소한의 정규식 패턴으로 함수를 추출하며, 복잡한 파싱은 향후 고도화 예정.

## 📝 TODO List
- [x] IR 구조체 확장 및 `ProjectIR` 개편
- [x] `IRChange` 및 `apply_changes` 엔진 구현
- [x] `Validator v2` (Component/Function/Dependency) 구현
- [x] `Patch-to-IR Transformer` 구현
- [x] `axon_patch_to_ir_mapping.md` 규약 문서화
- [x] `axon_validator_v2.md` 규약 문서화
- [x] Junior Agent 시스템 프롬프트 IR-First 기조로 개편
- [ ] 파이프라인 전체 동작 검증 (Test Run)
- [ ] (Next) Daemon의 Stage 6을 Rust 기반 IR Pipeline으로 완전 교체

---

## 📝 Post-Work Log (작업 후 기록)
- **2026-05-01**: 파일 중심에서 IR 중심으로의 아키텍처 피벗 완료. 모든 핵심 데이터 구조와 변환 엔진이 Rust로 구현되었으며, 이제 AXON은 코드 생성이 아닌 '아키텍처 변형' 시스템으로 동작함.
- **2026-05-01 (Hardening)**: 출력 규약(Output Contract) 강화. JSON 블록을 SSOT로 선언하고, 시그니처 표준화(타입 힌트 제거) 및 결정론적 정렬(Alphabetical Sort)을 적용하여 파이프라인 하류의 안정성 확보.
- **2026-05-01 (Contract Closed)**: AXON Spec Contract 정식 폐쇄. 아키텍처 생성 프롬프트를 'Golden Reference' 규격으로 고정하여 결정론적 정체성을 확립함.
- **2026-05-01 (Auto-Learning)**: 실패 기반 자동 학습 루프(Auto-Learning Loop) 구현. 로그를 분석하여 반복되는 실패(JSON 에러, 언어 혼용 등)를 규칙(Constraint IR)으로 축적하고 프롬프트에 자동 주입하는 RuleRegistry 시스템 구축.
- **2026-05-01 (Evolutionary Architecture)**: Rule Engine(Daemon), Prompt Composer(Agent), Failure Clusterer(Daemon) 통합 아키텍처 구현. 유사 실패를 시그니처 기반으로 클러스터링하고 임계값 도달 시 자동으로 규칙을 생성하는 '자가 진화' 체계 완성.
- **2026-05-01 (Rule Lifecycle)**: Rule Optimizer 구현. 규칙 압축(Normalization/Merging)을 통한 토큰 최적화, Polarity 기반 충돌 해결(Conflict Resolution), 멀티 에이전트 간 Global Registry 공유 체계를 통해 규칙의 생애주기를 시스템적으로 완성함.
- **2026-05-01 (Constraint Promotion)**: Rule → IR Constraint 승격. 문자열 기반 규칙을 구조화된 `Constraint` enum으로 변환하고, Validator v2와 연동하여 AST/IR 레벨에서 제약 조건을 강제하는 결정론적 검증 레이어 구축.
- **2026-05-01 (IR Strengthening)**: 비정형 규칙의 타입 시스템 통합. `map_rule_to_constraint`를 통해 규칙의 의미를 결정론적으로 해석하고, Validator가 이를 기반으로 실질적인 아키텍처 제어를 수행하도록 강화함.
- **2026-05-01 (AST Enforcement)**: AST 기반 시그니처 추출 및 Strict 매칭 파이프라인 완성. `FunctionSig`를 이용한 1:1 정밀 대조를 통해 설계와 구현의 불일치를 원천 차단하는 결정론적 집행력(Enforcement) 확보.
- **2026-05-01 (AST Pipeline Stages)**: AST 검증 파이프라인 4단계(Parse → Extract → Transform → Match) 구조 확립. `RawFunction` 추출과 `FunctionSig` 정규화 단계를 명확히 분리하여 검증 엔진의 디버깅 가능성과 타입 안전성을 강화함.
- **2026-05-01 (Visitor Extractor)**: Visitor 패턴 기반 AST Extractor 구현. 재귀적 탐색을 통한 중첩 함수 지원 및 `*args`, `**kwargs`, `kwonlyargs` 등 파이썬 가변 인자 형식의 결정론적 추출 기능 추가.
- **2026-05-01 (Structured Error Handling)**: 통합 검증 에러 핸들링 시스템 구축. 실제 가동 루프(Parse → Extract → Match)에서 발생하는 실패 유형들을 `ValidationError`로 구조화하여 시스템의 관측 가능성과 진단 정확도를 향상시킴.
- **2026-05-01 (Hierarchical Validator Reorganization)**: Validator 시스템의 계층화 구조(Core, Analysis, Integration) 확립. 데이터 타입, 분석 로직, 실행 오케스트레이션 레이어를 분리하여 유지보수성과 확장성을 극대화함.
- **2026-05-01 (ToT Phase 1 & 2 Verification)**: AST 파이프라인 실증 완료. `test_pipeline_alive`, `test_nested_extraction`, `test_js_intrusion_fails_parse` 등의 테스트를 통해 파이썬 파싱 및 함수 추출 기능의 정합성을 증명하고, '살아있는 파이프라인' 기반을 확보함.
- **2026-05-01 (Observability & Scenario Matrix)**: 핵심 엔진 관측 가능성 확보. `debug_print_ast` 유틸리티 도입으로 AST 시각화 기반을 마련하고, 시나리오 매트릭스 테스트(정상/누락/불일치)를 통해 Spec Matcher의 결정론적 정확도를 검증함.
- **2026-05-01 (Automated Debugging & Learning Loop)**: 실패 자산화 시스템 구축. `ExecutionArtifact` 기반의 공통 데이터 계약을 수립하고, 데몬의 검증 실패 시 자동으로 AST 시각화, 테스트 케이스 생성, 커버리지 업데이트를 수행하는 자가 학습 루프를 가동함.
- **2026-05-01 (Deterministic Clustering & Rule Generation)**: 자동화된 실패 분석 엔진 구축. `Analyzer` 인터페이스를 통해 독립적인 실패 클러스터링(Hashing 기반) 및 규칙 자동 생성(Pattern 매핑) 기능을 구현하여 지능형 진단 레이어를 강화함.
- **2026-05-01 (Stateful Intelligence Layer)**: 지능형 규칙 관리 체계 확립. `RuleRegistry`를 통한 규칙 SSOT 구축 및 `RuleEngine`의 점수 산정(Scoring), 감쇄(Decay: Time/Failure 기반) 알고리즘을 도입하여 규칙의 생명주기를 지능적으로 제어함.
- **2026-05-01 (Decision Layer Pipeline)**: 지능형 의사결정 파이프라인 완성. 신뢰 기반의 제약 조건 승격(Promotion), 프롬프트 최적화를 위한 Top-K 선별(Selection), 그리고 프로젝트 간 지식 공유를 위한 글로벌 동기화(Global Sync)를 하나의 결정론적 레이어로 통합함.
- **2026-05-01 (Deterministic Validator & IR Integration)**: IR 기반의 무결성 검증 파이프라인 구축. `ProjectIR`이 학습된 제약 조건을 영구적으로 흡수하고, `DeterministicValidator`가 이를 바탕으로 LLM 없이도 정확한 구조적 판정을 내리는 SSOT 체계를 확립함.
- **2026-05-01 (IR Parser Extension & Constraint Serde)**: 아키텍처 명세 파서 고도화. `architecture.md` 내의 `AXON:CONSTRAINTS` 블록을 자동으로 파싱하여 IR에 주입하고, `serde` 태그 기반의 안정적인 제약 조건 직렬화/역직렬화 체계를 구축함.
- **2026-05-01 (Atomic Write Pipeline — Staging / Commit / Writer)**: 다중 상태 충돌 원인 제거. RuleEngine을 side-effect 없는 Proposal 생성기로 한정하고, `ConstraintProposal` 스테이징 큐 → `IRCommitLayer` 단일 진입점 → `ArchitectureWriter` 단방향 렌더러로 쓰기 경로를 직렬화하여 race condition을 원천 차단함.
- **2026-05-01 (Constraint Priority System)**: 제약 조건 우선순위 체계 확립. 규칙 점수(score)를 기준으로 Low/Medium/High/Critical 4단계 등급을 자동 부여하며, 검증 시 Critical 제약이 항상 먼저 실행되도록 정렬 로직을 구현함.
- **2026-05-01 (IR Diff-based Learning)**: IR 스냅샷 간 변경 사항을 감지하는 `IRDiffEngine` 구현. 제약 조건 또는 컴포넌트가 사라지는 퇴보(Regression)를 자동으로 감지하고, 실패 신호를 추출하여 RuleEngine의 학습 데이터로 공급하는 피드백 루프를 완성함.
- **2026-05-01 (Full Auto-Evolution Pipeline)**: `EvolutionPipeline` 구현으로 완전 자동화된 자가 진화 루프 완성. `IR Diff 감지 → 신호 추출 → RuleEngine 학습 → 제안 생성 → 원자적 IR 커밋 → 마크다운 재생성` 6단계를 단일 `run()` 호출로 완성하는 엔터프라이즈급 자동화 체계를 확립함.
- **2026-05-01 (M1 — ConstraintMeta & Priority)**: 제약 조건 메타데이터 시스템 구현. 각 constraint에 `priority/confidence/hits/failures/last_used`를 추적하는 `ConstraintMeta`를 도입하고, 검증 결과에 따라 가중치를 자동 갱신하는 `update()` 로직을 확립함. merge tie-break 및 prune 판단의 정량적 기준으로 작동함.
- **2026-05-01 (M2 — IRDiff Learning with Frequency Gate)**: IR spec과 실제 추출된 코드 간의 구조적 차이를 `MissingFunction / ExtraFunction / SignatureMismatch` 3종으로 정의하는 `IRDiff`를 구현함. 동일 패턴이 임계치(N=3) 이상 반복된 경우에만 `ConstraintProposal`을 생성하여 노이즈 학습을 원천 차단함.
- **2026-05-01 (M3 — Validation Orchestrator)**: `on_validation_cycle` 오케스트레이터 완성. diff → 빈도 기반 제안 → 원자적 IR 커밋 → ConstraintMeta 갱신 → 주기적 pruning(10 commits마다)을 단일 함수로 연결함. validator/RuleEngine이 IR을 직접 수정하는 경로를 완전히 차단하고, 단일 쓰기 경로(IRCommitLayer)만을 허용하는 결정론적 아키텍처를 완성함.
- **2026-05-01 (Bugfix — extract_json [!NOTE] misparse)**: `extract_json`이 GitHub 마크다운 알림 문법(`[!NOTE]`, `[!TIP]` 등)의 `[`를 JSON 배열 시작으로 오인하는 버그 수정. `[` 다음 문자를 검사하여 `!`로 시작하는 패턴을 JSON이 아닌 것으로 판별하도록 개선함. 수정 파일: `axon-agent/src/lib.rs`
- **2026-05-01 (Bugfix — LLM array→HashMap IR parse)**: LLM이 `"components": [...]` 배열 형식으로 반환한 JSON을 `ProjectIR`의 `HashMap`으로 역직렬화 시 `invalid type: sequence, expected a map` 오류 발생 수정. `parse_ir_from_llm_json` 헬퍼를 도입하여 배열→HashMap 변환을 명시적으로 처리하고, 빈 signature/file 필드 자동 보완 로직 추가. 수정 파일: `axon-agent/src/lib.rs`
- **2026-05-01 (Bugfix — Stage 5 main.py 무한 실패 루프)**: IR에 `main` 컴포넌트가 없거나 `.rs` 파일만 생성된 경우 Stage 5 실행 검사가 매 retry마다 실패하는 무한 루프 버그 수정. Stage 3.5 직후 `main.py` 진입점 보장 로직을 추가하여, 항상 실행 가능한 최소 entry point를 자동 생성함. 수정 파일: `axon-daemon/src/lib.rs`
- **2026-05-01 (Bugfix — SSOT Physical Commit Failure)**: 시니어 승인 후 Registry Promotion은 수행되나, 실제 물리적 작업 디렉토리의 Stub 파일이 실제 코드로 교체되지 않던 'Stub Persistence' 버그 수정. Promotion 직후 `final_simulated_state`를 기반으로 물리적 파일 쓰기(Materialization) 로직을 복구함. 수정 파일: `axon-daemon/src/lib.rs`

---

## 🏛️ M4: Physical Verification Pipeline (Optimistic Automation)
**"물리와 논리의 일치, 그리고 실물 환경에서의 최종 무결성 보장."**

### 1. 전략: 낙관적 자동화, 비관적 개입 (Optimistic Automation, Pessimistic Intervention)
- **기계 검증 95%**: 파일 경로, 권한, OS 환경, 의존성 해결 등 물리적 배포 단계에서 발생하는 모든 노가다성 검증은 `PHYSICAL_VALIDATE` 레이어에서 자동으로 수행.
- **시니어 개입 5%**: 물리 하네스가 성공하면 시니어를 방해하지 않고 즉시 완료(Finalize). 실패 시에만 시니어에게 "물리적 환경 부적합" 인터럽트를 전송하여 수동 개입 및 롤백 수행.

### 2. 파이프라인 확장
1. **AXON_PASS (Logical Approval)**: 주니어의 제안서가 로직 및 사양을 충족함을 시니어가 1차 승인.
2. **MATERIALIZE_FS (Physical Commit)**: 승인된 코드를 실제 프로젝트 디렉토리에 물리적으로 작성.
3. **PHYSICAL_VALIDATE (Final Harness)**: 작성된 실물을 대상으로 실제 런타임 환경 검증 실행.
4. **FINAL_GATE (Auto/Manual)**: 성공 시 자동 Lock-in, 실패 시 시니어에게 제어권 이관.

### 3. 기술적 방어선 (Bug Eradication)
- **Stub Detection**: `axon_patch_simulator.py` 레벨에서 `# AXON STUB` 문구 잔존 여부를 감시하여 게으른 구현을 원천 차단.
- **Auto Rollback**: 물리 검증 실패 시, 작업 디렉토리를 오염시키지 않도록 즉시 이전 상태로 복구(구현 예정).

📝 **Post-Work Log (M4 Update)**
- **2026-05-01 (M4 — Physical Verification Pipeline)**: '낙관적 자동화, 비관적 개입' 원칙을 이식함. 시니어 승인 → 물리 파일 작성 → 실물 하네스 검증 → 자동 확정 파이프라인을 구축하여 공장의 수율과 안정성을 동시에 확보함.
- **2026-05-01 (Documentation — README Architecture Diagram)**: 시스템의 논리-물리 분리 검증 구조를 시각화한 개념도(`asset/mermaid-diagram.png`)를 README.md에 반영함.

---

## 👴 Senior Gate: Reject Rules (v0.1)
**"시니어 게이트는 최종 아노말리(Anomaly) 탐지기이며, 결코 공장의 병목이 되어서는 안 된다."**

### 1. 거절 철학 (Gate Philosophy)
- **중복 검증 금지**: 기계 검증(Axon, Physical Harness)이 이미 증명한 논리는 다시 확인하지 않는다.
- **고차원적 집중**: 아키텍처 무결성, 부작용(Side-effects), 장기적 유지보수성에만 집중한다.

### 2. 핵심 거절 조건 (Critical Rejections)
- **R1 (Architecture)**: ECS 원칙 파괴, 레이어 간 결합 발생 시 즉시 거절.
- **R2 (Side Effects)**: 명시되지 않은 파일 쓰기, 전역 상태 변이, 은밀한 DB 접근 금지.
- **R4 (Env Leak)**: 하드코딩된 경로, OS 종속적 가정, 누락된 의존성 선언 발견 시 거절.
- **R7 (Safety)**: critical path에서의 `unwrap()`, `panic!` 사용 및 부실한 에러 전파 거절.

### 3. 운영 제약 (Operational Constraints)
- **신속성**: 최대 리뷰 시간 2분 이내.
- **단순성**: 최대 거절 사유 3개 이내.
- **낙관적 편향**: 모호할 경우 흐름(Flow)을 위해 `APPROVE`를 우선한다.

---

📝 **Post-Work Log (Senior Gate Update)**
- **2026-05-01 (M4 — Senior Gate Integration)**: `senior_gate_reject_rules_v0.1.md`의 엄격한 거절 수칙을 마일스톤에 통합함. 시니어 에이전트는 이제 이 수칙에 근거하여 '물리 검증'을 통과한 결과물을 최종 심사함.
- **2026-05-01 (Technical — Auto-Rollback Implementation)**: 물리 검증 실패 시 작업 디렉토리를 오염시키지 않고 즉시 이전 상태로 복구하는 '자동 롤백' 로직을 `axon-daemon`에 이식 완료함.

---

## ⚙️ Physical Validator: Checklist (v0.1)
**"샌드박스의 성공이 실물 환경에서도 재현됨을 보장하는 자동화 레이어."**

### 1. 검증 범위 (Validation Scope)
- **F1~F2 (물리적 존재)**: 파일 생성 여부, 인코딩(UTF-8), 0-바이트 파일 방지.
- **F3~F5 (구조 및 빌드)**: 진입점(main) 존재 여부, 빌드/컴파일 성공, 의존성 해결.
- **F6~F8 (실행 및 링크)**: 런타임 크래시 여부, 모듈 링크 상태, 경로 정규화.
- **F9~F10 (고급 검증)**: 허용된 디렉토리 외 I/O 차단, 결정론적 리플레이(동일 입력/출력) 확인.

### 2. 실패 처리 (Failure Protocol)
- **Fast-Fail**: 단 하나의 체크라도 실패 시 즉시 파이프라인 중단.
- **Auto-Rollback**: 오염된 파일을 즉시 폐기하고 이전 상태로 복구.
- **Structured Log**: 실패한 체크 항목(F1~F10)을 명시한 구조화된 에러 리포트 생성.

---

📝 **Post-Work Log (Physical Checklist Update)**
- **2026-05-01 (M4 — Physical Checklist Integration)**: `physical_validator_checklist_v0.1.md`의 자동화 체크리스트를 마일스톤에 통합함. 
- **2026-05-01 (Next Step — Harness Hardening)**: 위 체크리스트 중 F9(I/O 경계) 및 F10(리플레이) 검증 로직을 `axon_execution_harness.py`에 이식하여 물리 검증 레이어의 정밀도를 높일 예정임.

---

## 🏛️ Master Spec: Axon IR v0.5 (Full Pipeline)
**"Logic → Reality → Human Sanity Check를 하나의 그래프로 통합한 검증 거버넌스."**

### 1. 파이프라인 노드 승격 (Node Elevation)
- **AXON_PASS**: 가상 샌드박스 검증 완료 상태.
- **MATERIALIZE_FS**: 실물 파일 시스템 배포 시스템 노드.
- **PHYSICAL_VALIDATE**: 실물 환경 하네스 검증 노드.
- **SENIOR_REVIEW**: 최종 인간 게이트웨이 제어 노드.
- **ROLLBACK**: 실패 시 상태 복원을 보장하는 제어 노드.

### 2. 파이프라인 무결성 보장 (Guarantees)
1. **논리적 정당성 (Axon)**: IR 제약 조건을 만족하는가?
2. **실행적 유효성 (Physical)**: 실제 런타임에서 크래시 없이 돌아가는가?
3. **구조적 안전성 (Senior)**: 아키텍처 미학 및 부작용 관점에서 안전한가?

### 3. 실패 처리 매트릭스 (Failure Matrix)
- **Materialize 실패**: FS 에러 발생 시 즉시 롤백.
- **Physical 실패**: 빌드/실행 실패 시 즉시 롤백 및 에러 리포트.
- **Senior 거절**: 설계 위반 시 즉시 롤백 및 시니어 인터럽트.

---

📝 **Post-Work Log (Master Spec v0.5 Integration)**
- **2026-05-01 (M4 — Master Spec v0.5 Alignment)**: `synapse_axon_ir_spec_v0.5.md`의 전체 파이프라인 규격을 마일스톤에 통합함. 오늘 구현된 `COMMIT_PENDING` 및 `Auto-Rollback` 로직은 이 스펙의 `MATERIALIZE_FS`와 `PHYSICAL_VALIDATE` 노드를 실체화한 것임.
- **2026-05-01 (Final State)**: AXON 공장은 이제 '논리-물리-인간'으로 이어지는 3단계 검증 거버넌스를 IR 레벨에서부터 실행 파일 배포까지 일관되게 준수함.
