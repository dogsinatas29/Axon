# SYNAPSE v0.0.24 Bootstrap Guide

------------------------------------------------------------------------

# 🚀 LLM Coding Principles (MANDATORY)

1.  Think Before Coding
2.  Simplicity First
3.  Minimal Changes
4.  Goal-Oriented Execution
5.  No Hallucinated APIs
6.  Stable Code Protection
7.  Context Confirmation

⚠️ You MUST follow these rules at every step. ⚠️ Do NOT modify unrelated
files. ⚠️ Do NOT expand scope.

------------------------------------------------------------------------

# 🧠 Bootstrap Strategy

Each phase MUST follow:

1.  Read document
2.  Restate Goal
3.  Plan (minimal)
4.  Implement (diff only)
5.  Create Test
6.  Run Test
7.  Verify success
8.  Move to next phase

------------------------------------------------------------------------

# 📌 Phase 1 --- Markdown Contamination Fix

## Document

v0.0.24_mkdn_solution.md

## Goal

-   markdown contamination = 0%

## Plan

-   Add validator rule for \`\`\` detection

## Implementation

-   Modify validator ONLY

## Test

``` python
def test_markdown_rejection():
    code = """```rust
fn test() {}
```"""
    assert validate(code) == False
```

## Success Criteria

-   markdown always rejected

------------------------------------------------------------------------

# 📌 Phase 2 --- Scope Control (Basic)

## Document

v0.0.24_scope_control_basic.md

## Goal

-   cross-file modification = 0%

## Plan

-   Add detect_scope_violation
-   Add write gate

## Implementation

-   Modify validator
-   Modify executor write

## Test

``` python
def test_scope_violation():
    code = "use crate::other;"
    assert validate(code) == False
```

## Success Criteria

-   cross-file attempts rejected

------------------------------------------------------------------------

# 📌 Phase 3 --- Scope Patch (Real Diff)

## Document

v0.0.24_scope_diff.md

## Goal

-   실제 코드에 적용

## Plan

-   Apply diff exactly

## Implementation

-   validator.py
-   executor.py

## Test

``` python
def test_write_gate(tmp_path):
    path = tmp_path / "a.rs"
    write_target_only(str(path), "fn a(){}")
    assert path.exists()
```

## Success Criteria

-   only target file written

------------------------------------------------------------------------

# 📌 Phase 4 --- Diff Enforcement

## Document

v0.0.24_diff_enforcement.md

## Goal

-   multi-file modification = 0%

## Plan

-   validate diff format

## Implementation

-   add is_single_file_diff

## Test

``` python
def test_invalid_diff():
    diff = """--- a.rs
--- b.rs"""
    assert is_single_file_diff(diff, "a.rs") == False
```

## Success Criteria

-   multi-file diff rejected

------------------------------------------------------------------------

# 📌 Global Validation Loop

After all phases:

Run 10 times:

-   no stub
-   no markdown
-   no scope violation
-   no diff violation

------------------------------------------------------------------------

# 📌 Failure Rule

If any test fails:

STOP Fix current phase Re-test

------------------------------------------------------------------------

# 📌 Final Goal

A fully controlled pipeline:

-   deterministic
-   single-file safe
-   markdown-free
-   no side effects

------------------------------------------------------------------------

# ✔ One-Line Principle

Control the behavior, not the model.
