# SYNAPSE v0.0.24 Scope Control Patch (Phase 1 + 3)

## Goal

Apply minimal changes to enforce: - Scope violation detection -
Single-file write gate

------------------------------------------------------------------------

## Patch Summary

-   Add detect_scope_violation to validator
-   Reject markdown and scope violations
-   Enforce write_target_only in executor

------------------------------------------------------------------------

## validator.py (diff)

``` diff
--- executor/validator.py
+++ executor/validator.py
@@
+def detect_scope_violation(content: str):
+    forbidden_patterns = [
+        "mod ",
+        "use crate::",
+        "../",
+        "std::fs::",
+        "File::open",
+        "File::create",
+    ]
+    return any(p in content for p in forbidden_patterns)

 def validate(content: str):
     if len(content) < 120:
         return False

     if "TODO" in content:
         return False

     if "fn " not in content:
         return False

+    if "```" in content:
+        return False
+
+    if detect_scope_violation(content):
+        return False

     return True
```

------------------------------------------------------------------------

## executor.py (diff)

``` diff
--- executor/executor.py
+++ executor/executor.py
@@
-        file_path = os.path.join(output_dir, node.file)
+        target_path = os.path.join(output_dir, node.file)

-        acquire_lock(file_path)
+        acquire_lock(target_path)

@@
-            if validate(content):
-                with open(file_path, "w") as f:
-                    f.write(content)
-                success = True
-                break
+            if validate(content):
+                write_target_only(target_path, content)
+                success = True
+                break

-        release_lock(file_path)
+        release_lock(target_path)
```

------------------------------------------------------------------------

## write_target_only

``` python
def write_target_only(target_path: str, content: str):
    import os
    os.makedirs(os.path.dirname(target_path), exist_ok=True)
    with open(target_path, "w") as f:
        f.write(content)
```

------------------------------------------------------------------------

## Result

-   Cross-file modification attempts rejected
-   Only target file is written
-   Markdown contamination blocked
