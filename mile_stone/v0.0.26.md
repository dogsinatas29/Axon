# encoding: utf-8
# 🚀 Milestone v0.0.26 - Axon Factory Hardening: The State Machine Era

---

## 🏛️ Sovereign Protocol - Master Hub Specification (v0.0.26)
**"모든 구현은 상태 머신 규약을 따르며, 모든 복구는 에스컬레이션 사다리와 힌트 주입을 통해 증명된다."**

### 1. 단일 진실 공급원 (SSOT) 체계
- **[architecture.md](file:///home/dogsinatas/rust_project/axon/workspace/architecture.md)**: IR 설계의 최종 결과물로, 모든 공정의 기준점이 됩니다.
- **모듈화 스펙**:
  - `decision.rs`: 상태 머신 코어, 실패 분류기, 힌트 생성기 통합.
  - `axon-agent/lib.rs`: **JSON Extractor**, **Empty Guard**, **Hint Injection** 프롬프트 엔진 통합.
  - `axon-daemon/lib.rs`: 부트스트랩 V3 상태 머신 루프 및 자가 치유 피드백 루프 완성.

### 2. 팀 기반 독재적 효율성 (Sovereign Authority)
- **에러 흡수 및 정제**: LLM의 비정형 출력(Markdown, Extra text)을 `extract_json` 필터로 정제하여 파이프라인 중단 방지.
- **폐쇄형 피드백 루프**: 실패 시 생성된 `Hint`를 다음 공정에 즉시 주입(Injection)하여 모델의 실수를 실시간 교정.

---

## 📅 작업 정보
- **상태:** ✅ Completed (Hardened)
- **관련 마일스톤:** [v0.0.25](file:///home/dogsinatas/rust_project/axon/mile_stone/v0.0.25.md)
- **목표:** **결정론적 IR 파이프라인의 물리적 안정화** 및 LLM의 I/O 계약 위반에 대한 완전한 면역력 확보.

## 🧠 상세 설계 및 로직

### 1. I/O 하드닝 (I/O Contract Hardening)
- **JSON Extractor**: `{`와 `}` 사이의 알맹이만 골라내는 핀셋 로직 적용.
- **Empty Output Guard**: 빈 응답 수신 시 즉시 차단 및 재시도 트리거.
- **Strict Prompting**: "Return ONLY valid JSON"을 최상위 제약 조건으로 명시.

### 2. 자가 치유 피드백 루프 (Self-Healing Loop)
- **Hint Injection**: `FailureCause`별 맞춤형 처방전을 다음 `generate_ir` 호출 시 프롬프트에 동적 주입.
- **Raw Logging**: 파싱 실패 시 **RAW LLM OUTPUT** 전체를 로그에 남겨 현장 보존 및 분석 가시성 확보.

### 3. 상태 머신 파이프라인 (Phase 1.4.x)
- `Skeleton` 단계에서의 무한 루프를 방지하기 위해 정제된 입력과 지능형 재시도 전략 결합.

## 🛠️ 기술적 변경 사항
- **Agent Update:** `AgentRuntime`에 `extract_json` 메서드 추가 및 `generate_ir` 힌트 주입 기능 이식.
- **Daemon Update:** `run_v3` 상태 머신에서 `current_hint` 관리 및 공정 간 피드백 배선 완료.
- **Syntax Fix:** 멀티라인 프롬프트 생성 과정의 구문 오류 완벽 해결.

## ⚠️ 예외 처리 및 주의 사항
- **Markdown Wrapping**: LLM이 ` ```json `을 포함하더라도 `extract_json`이 이를 자동으로 제거함.
- **Empty Response**: 빈 문자열 수신 시 `EMPTY_LLM_OUTPUT` 진단 후 `Skeleton` 재시도.

## 📝 Post-Work Log (작업 후 기록)
- 2026-05-06: 부트스트랩 V3 상태 머신 통합 완료.
- 2026-05-06: **JSON Extractor 및 힌트 주입 하드닝 완료.** (무한 루프 문제 해결)
- 2026-05-06: 모든 구문 오류 해결 및 `cargo check` 통과.
