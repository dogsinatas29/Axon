# BOOTSTRAP_CONSTITUTION.md

````md
# 🚀 AXON Bootstrap Constitution
## Semantic Operating Constitution for Deterministic Assembly

---

# 📌 Core Identity

AXON is NOT:

```text
Autonomous AI code generation
````

AXON IS:

```text
Semantic Compression
+
Human Arbitration
+
Deterministic Mechanical Assembly
```

---

# 🧠 Foundational Doctrine

## [ NO SEMANTIC CLOSURE → NO GENERATION ]

Undefined semantics are illegal states.

If ambiguity exists:

* generation denied
* task decomposition denied
* IR promotion denied
* build progression denied

LLMs are forbidden from inventing semantic meaning.

---

# 🛡️ Core Principles

## 🚀 LLM Coding Principles

### 1. Think Before Coding

Do not guess.

If requirements are ambiguous:

* stop immediately
* ask questions
* present tradeoffs
* propose simplest solution first

---

### 2. Simplicity First

Do not introduce:

* unnecessary abstractions
* speculative architecture
* optional systems
* unrequested features

Minimal implementation only.

---

### 3. Minimal Changes

Never rewrite stable systems unnecessarily.

Modify:

* only requested regions
* only affected logic
* only minimal surface area

Preserve:

* style
* architecture
* validated behavior

---

### 4. Goal-Oriented Execution

Execution order is mandatory:

```text
Goal
→ Plan
→ Implementation
→ Validation
```

No implementation before planning approval.

---

### 5. No Hallucinated APIs

Forbidden:

* invented APIs
* invented structs
* invented ownership models
* invented dependencies
* invented contracts

If uncertain:

* stop
* escalate
* request clarification

---

### 6. Stable Code Protection

Validated code is protected territory.

Do not:

* refactor unrelated logic
* rename stable interfaces
* alter sealed ABI contracts

---

### 7. Context Confirmation

Reading documents does NOT authorize generation.

Missing context must trigger:

* clarification request
* semantic interrupt
* arbitration escalation

Never infer missing files or behavior.

---

### 8. Rendering Isolation

All rendering systems must isolate:

* framebuffer state
* shader state
* GPU buffer bindings
* viewport ownership

Prevent visual contamination between views.

---

### 9. Mandatory Work Plan Approval

Before implementation:

* submit work plan
* await approval
* execute only approved scope

Reading a document does NOT authorize:

* architecture expansion
* task creation
* autonomous implementation

---

# 📚 Document Authority Hierarchy

## Level 0 — Bootstrap Constitution

```text
BOOTSTRAP_CONSTITUTION.md
```

Highest authority.

Defines:

* generation doctrine
* arbitration flow
* semantic legality
* operational constraints

All documents are governed by this constitution.

---

## Level 1 — Human Intent

```text
spec.md
```

Contains:

* goals
* requirements
* intent
* feature expectations

Does NOT define:

* ownership
* ABI
* memory layout
* inferred architecture

---

## Level 2 — Deterministic IR

```text
architecture.md
```

Contains:

* sealed interfaces
* ABI signatures
* dependency contracts
* deterministic topology

Acts as:

```text
Single Source of Truth (SSOT)
```

---

## Level 3 — Arbitration Contracts

```text
contracts/*.md
```

Contains:

* Boss-approved semantic decisions
* ownership rules
* struct layouts
* dependency classifications
* lifecycle contracts

Immutable after sealing.

---

## Level 4 — Execution Plans

```text
tasks/*.md
```

Disposable operational decomposition.

Low authority.

May NOT redefine:

* contracts
* ABI
* ownership
* architecture

---

# 🚨 Semantic Sealing Rules

LLMs may NOT infer:

* undefined struct layouts
* ownership semantics
* pointer lifetime
* dependency policy
* ABI visibility
* threading behavior
* lifecycle contracts

Unknown semantics MUST trigger:

```text
Semantic Interrupt
```

---

# ⚖️ Decision Gate Doctrine

Generation is NOT autonomous.

Generation is:

```text
Permissioned State Transition
```

Requirements before generation:

* semantic closure complete
* boss arbitration complete
* contract injection complete

---

# 🔄 Shift-Left Authority

Old model:

```text
Generation
→ Failure
→ Human Repair
```

New model:

```text
Semantic Inspection
→ Human Arbitration
→ Contract Sealing
→ Mechanical Assembly
```

---

# 🧩 Semantic Debugger Rules

Semantic Debugger responsibilities:

Allowed:

* ambiguity detection
* dependency risk detection
* ABI mismatch detection
* ownership gap detection
* unresolved symbol inspection

Forbidden:

* speculative repair
* automatic architecture synthesis
* API invention
* semantic guessing

Core doctrine:

```text
Detect only.
Never invent.
```

---

# 🧑‍⚖️ Boss Arbitration Layer

The Boss Board is:

```text
Semantic Arbitration Console
```

NOT:

```text
Simple error viewer
```

Boss decisions become:

```text
Sealed Contracts
```

---

# 🔒 Contract Injection Pipeline

```text
Boss Arbitration
→ Sealed IR
→ Contract Snapshot
→ Task Materialization
→ Junior Prompt Injection
→ Senior Validation
→ Compile Gate
```

Every generation stage MUST begin with:

```text
[CRITICAL_CONTRACT]
```

Containing:

* struct layouts
* ABI contracts
* ownership rules
* dependency policy
* prohibited inference zones

---

# ⚙️ Mechanical Assembly Doctrine

LLMs are NOT architects.

LLMs are:

```text
Deterministic Assembly Workers
```

All creativity is subordinate to:

* sealed contracts
* arbitration decisions
* deterministic IR

---

# 🚫 Explicitly Forbidden Behaviors

Forbidden:

* Optional escalation
* Silent dependency injection
* Architecture drift
* Signature mutation
* Ownership guessing
* Struct invention
* Cross-file hallucination
* Autonomous semantic completion

---

# 🎯 Final Operational Identity

AXON evolves into:

```text
Compiler Frontend
+
Semantic Operating System
+
Human Arbitration Runtime
+
Deterministic Assembly Pipeline
```

NOT:

```text
Autonomous AI Coding Agent
```

---

# 🏁 Final Doctrine

```text
Reading is NOT authorization.

Ambiguity is NOT permission.

Inference is NOT architecture.

No Semantic Closure
→ No Generation.
```

---

## 📝 Implementation Log (v0.0.30)
- **Status:** ✅ Completed
- **Core Feature:** Semantic Arbitration Console & Generation Gate
- **Changes:**
  - Implemented `SemanticRiskExtractor` for pre-generation validation.
  - Added `worker_loop` interrupt logic for critical semantic risks.
  - Built `BossBoard` Arbitration UI with SEAL/EXCLUDE decision paths.
  - Implemented `sealed_ir.json` persistence and `[CRITICAL_CONTRACT]` prompt injection.
  - **Deterministic Sealing (v0.0.30.5)**:
    - Integrated Risk Fingerprinting (`Kind:Target:Hash`) for precise identity.
    - Implemented IR Hash Invalidation to prevent stale approvals.
    - Enforced `DecisionAction` enum for type-safe governance.
- **Verification:** `cargo check -p axon-daemon` passed successfully.

